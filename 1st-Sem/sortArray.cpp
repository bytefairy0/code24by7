#include <iostream>
using namespace std;

int main(){

    // Sort the array
    int arr[] = {5, 2, 8, 1, 9};
    int len = sizeof(arr)/sizeof{arr[0]};

    int sortedArr[len];

    for (int i=0; i<len; i++){
        for (int j=0; j<len; j++){
            if (arr[j] < arr[i]){
                if arr[j] 
                sortedArr[i] = arr[j];
                
        }
    }

    return 0;
}